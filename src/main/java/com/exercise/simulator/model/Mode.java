package com.exercise.simulator.model;

import java.util.List;

public enum Mode {
	NORMAL,
	SPORT,
	SAFE;
	
//	public static Mode valueOf(String value) {
//		if (value.equalsIgnoreCase(NORMAL.toString())) {
//			return Mode.NORMAL;
//		} else if (value.equalsIgnoreCase(SPORT.toString())) {
//			return Mode.SPORT;
//		} else if (value.equalsIgnoreCase(SAFE.toString())) {
//			return Mode.SAFE;
//		}
//	}
}
